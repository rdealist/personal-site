# 2026-01-31 改进记录

## 本次改进

### 修复 i18n 硬编码问题
**文件变更**:
- `src/messages/zh.json` - 添加 `copy.code` 和 `copy.copied` 翻译键
- `src/messages/en.json` - 添加对应英文翻译
- `src/components/notes/code-block.tsx` - 使用 `useTranslations("copy")` 替代硬编码中文

**问题**: CodeBlock 组件中的复制按钮 aria-label 硬编码了中文 "复制代码" 和 "已复制"，导致英文界面显示中文按钮提示。

**解决**: 添加国际化翻译支持，现在会根据当前语言显示正确的按钮提示。

**部署状态**: 代码已推送到 GitHub，Vercel 将自动部署。

---

## 发现的其他优化点

### 1. next.config.ts 优化
当前配置文件几乎是空的，可以添加：
- 压缩优化 (`compress`、`swcMinify`)
- 图片优化配置
- 静态资源缓存策略

### 2. SEO 增强
- 添加 Open Graph 图像
- 增强 metadata 配置
- 添加 JSON-LD 结构化数据

### 3. 性能优化
- 3D 组件懒加载 (Hero 场景)
- 图片使用 Next.js Image 组件优化
- 字体优化 (next/font)

### 4. 用户体验
- 页面过渡动画 (Framer Motion)
- 骨架屏加载状态
- 深色/浅色模式切换优化

### 5. 可访问性
- 所有图片添加 alt 文本
- ARIA 标签完整性检查
- 键盘导航支持

---

## 后续建议

按优先级排序：
1. **高优先级**: SEO 增强 (对个人网站很重要)
2. **中优先级**: next.config.ts 性能配置
3. **低优先级**: 页面过渡动画
